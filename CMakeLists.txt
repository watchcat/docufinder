cmake_minimum_required(VERSION 2.8)
project( docufinder )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( docufinder src/docufinder.cpp )
target_link_libraries( docufinder ${OpenCV_LIBS} )
